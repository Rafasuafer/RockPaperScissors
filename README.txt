These were the local ports used:

For web app: localhost:4200
* The Angular web app is run separately with the command: ng serve GoDWeb

For web api: localhost:44394 
* The api port depends on running from IIS Express or GoDAPI, I've used IIS Express.
